# USB configurations
# CONFIG_USB is not defined in Zephyr 3.5.0
CONFIG_USB_DEVICE_STACK=y
CONFIG_USB_DEVICE_HID=y
CONFIG_USB_DEVICE_PRODUCT="Icebreaker Keyboard"
CONFIG_USB_DEVICE_VID=0x1915
CONFIG_USB_DEVICE_PID=0x0001
CONFIG_USB_DEVICE_MANUFACTURER="Icebreaker"
CONFIG_USB_DC_STM32=y

# Enable GPIO
CONFIG_GPIO=y

# Enable SPI for LED strip
CONFIG_SPI=y
CONFIG_LED_STRIP=y

# Enable clock control
CONFIG_CLOCK_CONTROL=y
# CONFIG_CLOCK_CONTROL_STM32 is not defined in Zephyr 3.5.0

# Enable pinctrl
CONFIG_PINCTRL=y

# Enable ZMK RGB underglow
CONFIG_ZMK_RGB_UNDERGLOW=y
CONFIG_WS2812_STRIP=y 